.worktrees/
.venv/
__pycache__/
*.pyc
*.egg-info/
.pytest_cache/

# Environment & secrets
.env
.env.*
*.log
*.pem
*.key
*.cert
*.p12
*.pfx
*.secret
credentials*.json
service-account*.json

# Claude Code local settings (per-machine, not for sharing)
.claude/

# Internal planning notes (contain local paths)
docs/superpowers/

# >>> GitSync-KDE (auto-managed — do not remove) >>>
# KDE folder icon (managed by GitSync-KDE)
.directory
# <<< GitSync-KDE <<<
